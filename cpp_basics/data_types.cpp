#include<iostream>
using namespace std;

int main() {
    // data types in cpp
    // 1 byte = 8 bits
    // - int -> 4 bytes (32 bits)
    // - float -> 4 bytes (32 bits)
    // - char -> 1 byte(8 bits)
    // - bool -> 1 byte(8 bits)
    // - double -> 8 bytes(64 bits)
    // Range for datatypes -> from -2^(n-1) to +2^(n-1)-1
    
    // modifiers for data types
    // unsigned
    // signed
    // long 
    // short
    long int b; // increase the range of int 
    short int a; // reduces the range of int from 4 bytes to 
    unsigned long int c; // kitne bhi variation krlo
    return 0;
}